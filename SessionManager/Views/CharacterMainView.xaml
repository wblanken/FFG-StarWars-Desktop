<UserControl x:Class="SessionManager.Views.CharacterMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:SessionManager.Controls"
             mc:Ignorable="d" Width="700"
             d:DesignHeight="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>  
        <controls:AttributesHeaderControl Grid.Row="0" Attributes="{Binding Character.Attributes}"/>
        <controls:CharacteristicsHeaderControl Grid.Row="2" Characteristics="{Binding Character.Characteristics}"/>
        <controls:SkillsControl Grid.Row="4"/>
    </Grid>
</UserControl>
