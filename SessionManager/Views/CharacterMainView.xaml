<UserControl x:Class="SessionManager.Views.CharacterMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SessionManager.Views"
             xmlns:controls="clr-namespace:SessionManager.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="1000" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>  
        
        <controls:CharacteristicsHeaderControl Grid.Row="1" Characteristics="{Binding Character.Characteristics}"/>
    </Grid>
</UserControl>
